# 불꽃캠프 사이트 — 프로젝트 컨텍스트 (Claude용)

이 파일은 Claude Code가 자동으로 읽는 프로젝트 설명서입니다.
이 폴더에서 Claude Code를 켜면, 별도 설명 없이 아래 내용을 맥락으로 작업을 시작합니다.

## 이 프로젝트가 뭔가
**불꽃캠프** — 장주혁(장쌤)의 사탐(사회문화·생활과윤리) 과외·모의고사 실전반 랜딩 사이트.
순수 정적 웹사이트이며 **GitHub Pages**로 배포됩니다.

## 기술 스택 (중요: 빌드 과정이 없음)
- **빌드 도구·프레임워크·npm 없음.** HTML 파일을 그대로 브라우저로 열면 동작합니다.
- **멀티페이지 구조**입니다. 상단바 메뉴 하나하나가 별도 HTML 파일이며, 스크롤 이동이 아니라 페이지가 바뀝니다.
- 사탐 쪽 페이지는 **공용 `style.css` + `app.js`를 링크**해서 씁니다 (CSS/JS 중복 없음).
- **예외: `korean.html`은 자기 안에 CSS를 전부 갖고 있습니다** (초록 테마 때문). 아래 주의사항 참고.
- 외부 의존성은 **Pretendard 폰트(jsdelivr CDN)** 하나뿐입니다.

## 파일 구조
| 파일 | 역할 |
|---|---|
| `index.html`   | **메인** — 히어로 + 결과 숫자 |
| `why.html`     | 왜 8시간이면 되나 |
| `course.html`  | 2단계 코스 (로드맵) |
| `program.html` | 프로그램 3종 탭 (불꽃캠프 / 사탐 특강 / 1:1 과외) |
| `compare.html` | 인강·학원 비교 |
| `proof.html`   | 작년 데이터 + 실제 사례 |
| `parents.html` | 학부모님께 + 장주혁 선생님 소개 |
| `info.html`    | 시간표·수강료 + 전화상담 신청 + FAQ |
| `korean.html`  | 불꽃캠프 국어 (이어진 선생님) — 헤더 '📗 국어' 버튼으로 진입 |
| `style.css`    | **공용 CSS** (korean.html 제외 전 페이지) |
| `app.js`       | **공용 JS** — 상단바 스크롤, 모바일 메뉴, 프로그램 탭 |
| `camp-cards.html` | 김과외 업로드용 그래픽 카드 (사이트와 별개) |
| `camp-og.svg`  | 공유 썸네일 이미지 |

**상단바 메뉴 7개 = 페이지 7개.** 왜 8시간→`why` / 2단계 코스→`course` / 프로그램→`program` /
인강 비교→`compare` / 작년 데이터→`proof` / 학부모님께→`parents` / 시간표·수강료→`info`
**상단바에는 `#앵커` 링크를 쓰지 마세요.** 같은 페이지 안에서 스크롤돼 버립니다 (전부 별도 페이지여야 함).

**페이지를 추가하려면:** ① 기존 페이지를 복사해 head·상단바·푸터를 그대로 두고 본문만 교체
② 모든 페이지의 `<nav>`와 `.msheet` 링크에 새 항목 추가 (9개 HTML 전부 — 놓치기 쉬움)

## ⚠️ 작업 시 반드시 알아야 할 점
- **디자인 토큰(`--fire`, `--deep`, `--soft` 등)은 `style.css`와 `korean.html` 두 곳에 있습니다.**
  색을 바꾸려면 **둘 다** 고쳐야 사탐/국어 페이지 색이 어긋나지 않습니다.
- **내비게이션 링크는 9개 HTML 파일에 복붙되어 있습니다.** 메뉴를 바꾸면 전부 고쳐야 합니다.
- **국어는 초록, 사탐은 주황.** 국어 페이지는 콘텐츠 영역(`#krmain`)과 히어로(`.krhero`)에만
  초록 토큰을 덮어씁니다. 상단바·헤더·푸터는 불꽃캠프 주황 그대로 둡니다 (같은 사이트로 보이게).
- **CSS 편집 후 중괄호 균형을 꼭 확인하세요.** 규칙을 지울 때 `@media(...){` 만 남으면
  그 뒤 스타일이 전부 무효화됩니다 (실제로 한 번 발생했던 사고).

## 주요 콘텐츠 (수정 시 참고)
- 강사 소개 제목: 사탐 = "장주혁 선생님 소개", 국어 = "이어진 선생님 소개"
- 불꽃캠프 과목: **사회문화 · 생활과윤리 · 윤리와사상** (특강·과외는 사문·생윤만)
- 수강료: 불꽃캠프 1과목 50만 / 두 과목 80만 / 온라인 불꽃캠프 월 40만·4회(8/15 개강, 토 13~17)
  / 사문 특강 3회 50만 / 국어 월 50만(5명 한정) / 1:1 과외 대면은 **상담 후 결정**, 화상 60만
- 연락처: 사탐·전체 문의 010-9306-5043(장쌤), 국어 010-3897-0646(이어진 선생님)
- **불꽃 모의고사 = OX 모의고사** (같은 것이므로 따로 나열하지 말 것)

## 로컬에서 보는 법
빌드가 없으므로 파일을 브라우저로 바로 열거나,
`python3 -m http.server 8000` 후 `http://localhost:8000` 으로 접속합니다.

## 작업 규칙
- 변경 후에는 실제로 브라우저에서 해당 페이지를 열어 깨지지 않는지 확인.
- 색/폰트/간격 등 토큰성 변경은 두 파일 전체 일관성 유지.
- 커밋 메시지는 무엇을 왜 바꿨는지 한국어로 명확하게.
